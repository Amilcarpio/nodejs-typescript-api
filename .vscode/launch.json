{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "runtimeExecutable": "nodemon",
            "restart": true,
            "skipFiles": ["<node_internals>/**"],
            "program": "${workspaceFolder}/src/server.ts"
        }
    ]
}